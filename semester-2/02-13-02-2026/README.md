# 🧩 Занятие 13.02.2026 (Определение типа последовательности)

[![C++](https://img.shields.io/badge/C++-20-blue.svg)](https://isocpp.org/)

Привет! В данной папке представлены программы, нацеленные на работу с последовательностями чисел и определение их типа (
монотонная, убывающая, возрастающая и т.д.). В решениях используется работа с побитовыми операциями и `enum class`.

---

## 📋 Содержимое папки

| Файл               | Описание                                                                                                                                                                                                                  |
|--------------------|---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| 📄 `ex.cpp`        | Черновой или базовый вариант определения типа последовательности через обычный `enum`. Присутствуют наброски логики переходов.                                                                                            |
| 📄 `ex1.cpp`       | Продвинутый вариант с использованием строгой типизации перечислений (`enum class`). Содержит реализацию перегрузки о��ераторов для безопасной работы с битовыми масками и функции для вывода последовательности на экран. |
| 📄 `notMyCode.cpp` | Хорошо документированная реализация (в стиле Doxygen) классификатора последовательностей целых чисел с использованием возможностей стандарта C++20 (в частности, `using enum`).                                           |

---

## 🛠 Технические особенности кода

### 1. Использование битовых масок

В файлах акт��вно используются битовые маски для хранения состояния последовательности:

```cpp
enum class TypeOfSequence {
    None   = 0,
    Equals = 1 << 0,
    Less   = 1 << 1,
    More   = 1 << 2,
};
```

Это позволяет комбинировать флаги (например, последовательность может быть одновременно `Less` и `Equals`, то есть
неубывающей).

### 2. Перегрузка операторов

В `ex1.cpp` и `notMyCode.cpp` показано, как правильно перегружать побитовый оператор `|` для `enum class`, чтобы
операции над флагами были безопасны с точки зрения типизации.

### 3. C++ 20 Features

В `notMyCode.cpp` применяется `using enum TypeOfSequence;`, что позволяет использовать значения перечисления без
постоянного указания области видимости, делая код чище.

