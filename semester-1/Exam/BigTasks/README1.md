план написания кода:

1)задать в main массив размера n.

2)написать прототипы функций.

    1.функция которая считает сколько цифр в числе.

    2.функция, которая определяет, является ли число 
    палиндромом(должна возвращать значение в формате bool).

    3.функция, которая проходит по массиву и определяет какие 
    ее элементы -- палиндромы. Затем добавляет их в сумму.

реализация функции которая определяет является ли число палиндромом:
пусть в числе n цифр

1)получаем первую цифру справа( делим с отсатком на 10)

2) первую цифру слева(делим на n)
3) сравниваем их, если они НЕравны, то return false, сли равны, то идем следующую иттерацию
4) избавляемся от двух чисел слева и справа:
   1.отнимаем остатака от деления 10
    2. число %= 10*(n-1)
    3. число /=10 
